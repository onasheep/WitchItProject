using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class TitleSceneControl : MonoBehaviour
{
    //[SerializeField] private GameObject introCanvas;
    //public bool _isPress = default;
    
    //// Start is called before the first frame update
    //void Awake()
    //{
    //    introCanvas = GameObject.Find("IntroCanvas");
        
    //}
    //// Update is called once per frame
    //void Update()
    //{
    //    _isPress = introCanvas.GetComponent<TitleOnOffControl>().isPress;
    //    if (_isPress == false && Input.anyKeyDown && introCanvas.GetComponent<TitleOnOffControl>().pressCount >=1)
    //    {
    //        SceneManager.LoadScene("LobbyScene");
    //    }
    //}
}
